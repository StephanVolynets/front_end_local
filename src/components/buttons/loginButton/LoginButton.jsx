'use client'

import styles from './LoginButton.module.css'

const LoginButton = () => {
  
  return (
    <button className={styles.button}>
      Login
    </button>
  )
}

export default LoginButton
